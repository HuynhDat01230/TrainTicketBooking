@model IEnumerable<TrainProject.Models.LoaiVe>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.MoTaLoaiVe)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.MoTaLoaiVe)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaLoaiVe }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaLoaiVe }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaLoaiVe })
        </td>
    </tr>
}

</table>
